# Sprint 11 — Stage B report: README + validation write-up + snippet + reconciliations

**Stage:** B (aether repo). **Status:** complete, STOP for review. **Branch:** `main`, not pushed.
All figures sourced from committed artifacts via the source-of-truth snippet (cardinal rule 5); no
persuasive/marketing register; the vision never appears as delivered (cardinal rule 2).

## Deliverables

### 1. Source-of-truth key-results snippet — `docs/key_results.json`
- **Extracted, not retyped.** Generated by `tools/build_key_results.py` (committed, regenerable:
  `uv run python tools/build_key_results.py`). Each figure carries `{value, display, source}` where
  `source` is the artifact path + locator; the script does the rounding so all three deliverables quote
  one identical `display` string. Stamped with `as_of_sha` (the aether commit the artifacts were read at).
- **F3 honored:** both Goturdepe flux calibrations ride the snippet — ours-cal **23.4 t/hr** and
  NASA-anchored **16.0 t/hr** — so no deliverable can silently pick one.
- **F4 honored:** the two literals are pinned from their committed artifacts — Permian plume-scale pixel
  r = **0.137** (`stage_b_outputs/permian_basin_2022/diagnostics.json#pixelwise_pearson_on_footprint_ours_vs_nasa`)
  and the Duren 2019 DOI **10.1038/s41586-019-1720-3**
  (`attribution_outputs/permian_basin_2022/hypotheses.json#magnitude_prior_basis`).
- **Staleness defense:** `tools/tests/test_key_results.py` (6 tests) re-runs the extraction and asserts
  the committed snippet still equals its artifacts (everything but the volatile SHA), plus pins the gate
  invariants (F3 dual flux, F4 literals) and the tier ceilings (methane never VALIDATED; heat C1/C2
  VALIDATED, C3/C4 honest negatives; negative UHI present). This is the proportionate control — a full
  cross-repo CI guard between arkaneworks and aether stays out of scope per the brief; the snippet + SHA
  stamp + the Stage D consistency read is the control.

### 2. README.md — honest dev-facing front door (full rewrite)
- The old README led with "AI-native dashboard … Earth-climate is the wedge. The platform is the
  ambition." and was stuck at "Sprints 1–7." Replaced wholesale. The new front door leads with the
  **traceability/honesty thesis**, links the live demo and states exactly what it serves (three events,
  two domains), gives the pipeline + architecture honestly (the AI layer is marked **specified, not
  built**), a **What is validated, and what is not** section (tier rubric, the +1.46×, the C3/C4
  failures, the negative UHI), a data-sources/licenses table (NOAA ISD non-redistributable, derived-only;
  OGIM CC BY 4.0; etc.), DOI citations, and the **Carbon Mapper relationship** stated honestly
  (complementary/inspired-by; shared JPL *heritage*, not pipeline equivalence; not affiliated/endorsed —
  gate item F5). Figures quote the snippet; no marketing register.

### 3. Scientific validation write-up — `docs/science/sprint11_validation_writeup.md`
- The rigor story in depth, every figure via the snippet/artifacts: the independent HITRAN2020/HAPI
  saturation-aware `k` (NASA repositioned as a shape cross-check, r = 0.993); the spatial-fidelity
  figures with exact definitions; the +1.46× systematic found, reproduced, **left uncorrected**, and its
  non-transfer to the Permian; the per-quantity tier rubric and why VALIDATED is reserved; the
  pre-registration discipline with the **C3/C4 failures as the headline demonstration of method
  maturity**; the two-lanes air-vs-skin separation; the factor-attribution boundary (presence/rarity not
  contribution; F5 counter-evidence; Zachariah cited-not-claimed). Uncertainty and coverage stated as
  findings.

### 4. Doc reconciliations
- **Test count fixed to current.** The Sprint 10 closeout figure is corrected from **388 → 389** (the
  environment drift flagged at session start). The current suite (Sprint 11 Stage B) is recorded
  separately with its composition (the +6 key-results guard tests; the +2 report-completeness cases that
  parametrize over the two new Sprint 11 reports).
- **Visual-fidelity verification harness added as a genuine deferred open-thread** in `HANDOFF.md` and
  `PROJECT_STATUS.md` — an *automated* computed-style / visual-diff guard for inspector blocks so an
  unthemed block fails red in CI without a human eye. This is **distinct from** the completed Sprint 10
  Stage C manual shot-list-criterion fix; the intent had been carried only in the reviewer's head and was
  never recorded. Recording it is the correction.

## Verification (re-run, not transcribed)
- `uv run pytest` → exit 0, **397 passed, 6 skipped, 7 deselected** (the 6 skips are the env-gated live
  guards; 7 deselected = the integration-marked set). Decomposition vs the 389 session baseline: +6
  (`tools/tests/test_key_results.py`) +2 (report-completeness over `sprint11_stage_a.md` and
  `sprint11_stage_a_gate.md`). This Stage B report adds one further report-completeness case; the final
  count is recorded in the reconciled docs.
- `uv run ruff check .` → exit 0 (All checks passed).
- `apps/web` not touched in Stage B (the case-study page is Stage C, static HTML on arkaneworks; no
  dashboard code changed) → `tsc`/`next build` unaffected, green from Sprint 10.
- `aether-eval` not run: no `packages/detection` or `packages/causal` change (Stage B is docs/tooling).

## Flags for the gate
- **Snippet `as_of_sha` self-reference (benign, by design).** The snippet stamps the SHA its figures were
  read at; committing it advances HEAD past that SHA (the same inherent nuance as the Sprint 10 deploy
  evidence — a file cannot name its own commit). The figures move only when an artifact moves; the guard
  excludes the SHA. Regenerate after any artifact change.
- **F5 (Carbon Mapper) lands in prose, not an artifact** — the README states the relationship boundary
  from public knowledge; it makes no Aether-measured claim about Carbon Mapper. Stage C must keep the
  same boundary in the case study.
- Nothing else cut or demoted; the ledger's Partition-1/2/3 figures all flow through the snippet unchanged.

**STOP for review.** Next: Stage C (revamp `ape.html` in place per the Stage A gate ruling — route
`/ape`, name kept, first sentence the honest hook, vision only as Aether's own labeled roadmap).
